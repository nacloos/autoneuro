[project]
name = "yang-tasks"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "neurogym @ git+https://github.com/neurogym/neurogym.git",
    "matplotlib",
    "gymnasium",
]
